# Local secrets

This directory is used for local Docker secret files. The Makefile creates missing `*.txt` files automatically with random values.

These files are intentionally ignored by git because the Inception subject forbids committing credentials.
